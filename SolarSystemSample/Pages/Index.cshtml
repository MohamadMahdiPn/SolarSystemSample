@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="solar-wrapper">
    <div class="solar-system" id="solarRoot">
        <!-- با JS ساخته می‌شود -->
    </div>
</div>
<div class="info-panel">
    <div>
        <div class="info-title">منظومه شی‌گرا 🌌</div>
        <div class="info-subtitle">خورشید، سیاره‌ها و قمرها فقط از روی داده ساخته می‌شوند.</div>
    </div>

    <div class="info-box">
        <h2 id="infoName">یک جرم را انتخاب کن</h2>
        <p id="infoDesc">روی خورشید، سیاره‌ها یا قمرها کلیک کن تا اطلاعاتشان را ببینی.</p>

        <div class="info-grid" id="infoGrid" style="display:none;">
            <div class="info-label">نوع:</div>
            <div class="info-value" id="infoType"></div>

            <div class="info-label">فاصله:</div>
            <div class="info-value" id="infoDistance"></div>

            <div class="info-label">دوره‌ی مداری / سال:</div>
            <div class="info-value" id="infoYear"></div>

            <div class="info-label">دوره‌ی چرخش / روز:</div>
            <div class="info-value" id="infoDay"></div>
        </div>
    </div>

    <div class="hint">
        🧠 برای ساخت منظومه خودت، فقط آرایه‌ی <code>systemConfig</code> در جاوااسکریپت را تغییر بده؛ به HTML دست نمی‌زنی.
    </div>
</div>